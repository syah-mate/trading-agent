# agents package — v2.1 Always-In Single Agent Architecture
#
# Active:
#   trading_agent.py  — single AI agent (LLM via system prompt)
#   mt5_executor.py   — MT5 order execution
#   orchestrator.py   — main loop (every 3 candles, always-in strategy)
